#ifndef FEATURE_H
#define FEATURE_H

#include <QObject>
#include<QString>

class feature : public QObject
{
    Q_OBJECT
public:
    explicit feature(QObject *parent = nullptr, QString new_name, QString new_address);
    void setName(const QString &);
    void setImage(const )

signals:

public slots:

private:
    QString name;
    QPixmap image;
};

#endif // FEATURE_H